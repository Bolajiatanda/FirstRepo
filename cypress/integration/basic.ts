// Assertion in cypress
/*Implicit: inbuilt assertions
Command in cypress like should() check for the expected condition 
without having to use any assert or expect command separately___should( )
and() */
// Explict: Not in-built or referrred 
//Have to use specific assertion commands like assert() or expect()

it('jyre test', function(){

    cy.visit('')
})